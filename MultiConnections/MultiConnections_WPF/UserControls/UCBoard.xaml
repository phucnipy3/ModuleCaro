<UserControl x:Class="MultiConnections_WPF.UserControls.UCBoard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiConnections_WPF.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="560" Height="560" HorizontalAlignment="Center" VerticalAlignment="Bottom">
    <materialDesign:Card>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="28"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <WrapPanel x:Name="wpnlCol" Grid.Row="1"/>

            <WrapPanel x:Name="wpnlRow" Grid.Column="1"/>

            <WrapPanel x:Name="wpnlBoard" Grid.Row="1" Grid.Column="1"/>
        </Grid>
        
        
    </materialDesign:Card>
</UserControl>
