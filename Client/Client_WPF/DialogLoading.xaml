<UserControl x:Class="Client_WPF.DialogLoading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client_WPF"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="Auto" Height="Auto">
    <Grid>
        <materialDesign:DialogHost x:Name="loading" Grid.Row="1">
            <materialDesign:DialogHost.DialogContent>
                <StackPanel Orientation="Horizontal">
                    <ProgressBar
                                      Style="{DynamicResource MaterialDesignCircularProgressBar}"
                                      HorizontalAlignment="Center"
                                      Margin="16"
                                      IsIndeterminate="True"
                                      Value="0" />
                    <TextBlock Text="Vui lòng đợi" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="Bold"/>
                </StackPanel>
            </materialDesign:DialogHost.DialogContent>

        </materialDesign:DialogHost>
    </Grid>
</UserControl>
